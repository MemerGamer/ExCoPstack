apiVersion: v1
kind: Secret
metadata:
  name: excop-secret
  namespace: excop
type: Opaque
stringData:
  # Example secret - in production, use proper secret management
  API_KEY: "excop-secret-key-2025"
  # Base64 encoded values can also be used
  # password: <base64-encoded-value>

